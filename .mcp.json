{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp@latest"],
      "description": "Browser automation for testing"
    },
    "filesystem": {
      "command": "npx",
      "args": ["@modelcontextprotocol/server-filesystem", "."],
      "description": "Enhanced file operations"
    }
  },
  "_comments": {
    "figma": "Add: claude mcp add --transport http figma https://mcp.figma.com/mcp",
    "github": "Add: claude mcp add --transport http github https://api.githubcopilot.com/mcp/",
    "database": "Add: claude mcp add db -- npx @bytebase/dbhub --dsn 'postgresql://...'",
    "sentry": "Add: claude mcp add --transport http sentry https://mcp.sentry.dev/mcp"
  }
}
